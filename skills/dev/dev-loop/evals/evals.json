{
  "skill_name": "dev-loop",
  "evals": [
    {
      "id": 0,
      "name": "csvstats-json-flag",
      "prompt": "Run an autonomous dev loop on this: add a --json flag to csvstats so it prints the stats as a JSON object instead of the text table. Keep the default text output unchanged.",
      "expected_output": "Feature branch with working --json flag, docs/<feature>/PLAN.md, LOOP.md, REVIEW.md with a passing verdict, tests still green, nothing pushed (paused for approval).",
      "files": ["fixtures/csvstats"],
      "assertions": [
        "--json flag works: `python3 main.py sample.csv price --json` on the work branch prints valid JSON containing column, count, min, max, mean, median",
        "default text output unchanged: running without --json still prints the original text format",
        "existing tests still pass on the work branch (python3 -m unittest)",
        "work done on a feature branch; main still points at the original fixture commit",
        "loop artifacts exist: docs/<feature>/PLAN.md, LOOP.md, and REVIEW.md containing a Machine-Readable Verdict section",
        "LOOP.md iteration table is filled in (verdict and severity counts recorded, not all dashes)",
        "no leftover worktrees and .worktrees/ is empty or absent",
        "no Co-authored-by trailers anywhere in git log",
        "nothing pushed and no PR attempted (loop stopped at approval pause)"
      ]
    },
    {
      "id": 1,
      "name": "loginapi-rate-limit",
      "prompt": "Loop on this until it passes review: add rate limiting to the /login endpoint — max 5 failed attempts per IP per minute, respond 429 after that. Successful logins reset the counter.",
      "expected_output": "Feature branch with working per-IP rate limiting, loop artifacts (PLAN.md, LOOP.md, REVIEW.md), tests covering the 429 path, nothing pushed (paused for approval).",
      "files": ["fixtures/loginapi"],
      "assertions": [
        "rate limiting works: the 6th failed login from one IP within a minute returns HTTP 429",
        "successful login resets the counter: after 4 failures then 1 success, the next failed attempt is 401, not 429",
        "existing tests still pass on the work branch (python3 -m unittest)",
        "new tests cover the 429 path",
        "work done on a feature branch; main still points at the original fixture commit",
        "loop artifacts exist: docs/<feature>/PLAN.md, LOOP.md, and REVIEW.md containing a Machine-Readable Verdict section",
        "LOOP.md iteration table is filled in (verdict and severity counts recorded, not all dashes)",
        "no leftover worktrees and .worktrees/ is empty or absent",
        "no Co-authored-by trailers anywhere in git log",
        "nothing pushed and no PR attempted (loop stopped at approval pause)"
      ]
    }
  ]
}
